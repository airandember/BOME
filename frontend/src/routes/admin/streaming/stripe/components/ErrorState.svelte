<script>
	export let error;
	export let loading;
	export let debugMode;
	export let debugInfo;
	export let lastActivity;
	export let dataTransferActive;
	export let keyType;
	export let loadingStatus;
	export let secret;
	export let summary;
</script>

<!-- Move your error UI here -->
